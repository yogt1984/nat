# Tasks — 2026-06-22

Outstanding work after the CLI-modularization / test-backlog / ML-algo-fix sprint.
Ordered by leverage against the binding constraint (data continuity) and shippability.

## P0 — Data continuity (the binding constraint)
- [ ] **T0b: provision Hetzner box + deploy redundant ingestor.** Removes single-point su-35
      risk; unblocks the Q-branch. Vehicle is ready (`nat deploy cloud`, `HETZNER_DEPLOYMENT_PLAN.md`).
      First step: provision box per the plan §1, then `nat deploy cloud <ip> --dry-run`.
- [ ] **Accept the 7-clean-day streak on the box**, then reconcile gap profiles vs su-35
      (plan §4–5). This is the gate that lifts the su-35 freeze.

## P1 — Real bugs / correctness (surfaced this session)
- [ ] **Retrain or revalidate the 3 ML algos against current schema.** Inference now feeds each model
      its trained `feature_names`; confirm the 2026-06-08 model artifacts are still right (or retrain
      on a clean day). Algos: `mean_reversion_detector`, `meta_labeling`, `regime_conditioned_lgbm`.
- [ ] **Fix `nat agent status` daemon `ModuleNotFoundError: logging_config`.** Pre-existing; blocks
      running agents (incl. on the cloud box). Likely a sys.path / entrypoint issue in the daemon.

## P2 — Test suite health (remaining failures are environment-gated, not test bugs)
- [ ] **Local redis/ingestor harness for the service-dependent suites** so they run in CI:
      `test_agent_monitor`, `test_daemon_integration`, `test_integration_profiling`,
      `test_tier2_swarm_integration`, `test_macro_agent`, `test_mf_agent`.
      First step: scope a docker-compose or pytest fixture that stands up redis + a stub ingestor.
- [ ] **Wire the `slow` marker into CI**: default fast lane runs `-m "not slow"`; nightly runs full.
      (Marker registered in `scripts/tests/conftest.py`; heavy clustering suites already tagged.)

## P3 — Platform / roadmap follow-ups
- [ ] **Publish the `.deb` to the private apt repo** for repeatable fleet installs
      (`packaging/README.md` §2), replacing scp+dpkg in `nat deploy cloud`.
- [ ] **T14 promotion daemon** — drive signal-lifecycle transitions automatically (DISCOVERED→…).
- [ ] **T5 agent `register_signal()` → lifecycle** integration (agent discoveries auto-seed the
      lifecycle as DISCOVERED).
- [ ] **D1 viz maturity tags** — surface unit maturity (ALPHA/BETA/…) in viz output.

## Notes
- Q-branch revalidation stays gated on the clean-data streak (P0) — do not start until a clean
  window exists. **No su-35 contact until the streak completes.**
