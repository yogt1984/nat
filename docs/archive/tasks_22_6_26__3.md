# Task — 2026-06-22 (#3): operationalize the 5D GMM regime classifier

## Problem
The GMM regime classifier is **wired-but-disabled and untrainable** on master:
- Rust inference is already correct 5D — `state/mod.rs:299-311` feeds
  `[kyle_lambda_100, vpin_50, absorption_zscore, hurst_300, whale_net_flow_1h]`, and whale flow is
  populated (commit `38aa7e1`).
- But `gmm_model_path` is commented out (`config/ing.toml:38`) → `state/mod.rs:81` never loads the
  classifier; no regime classification runs.
- No `models/regime_gmm.json` exists (build artifact, never generated/committed).
- `scripts/train_regime_gmm.py` `FEATURE_COLUMNS` use **non-existent column names**
  (`ill_kyle_lambda_300`, `tox_vpin_50`, `regime_absorption_zscore_300`, `whale_net_flow_4h_norm`) →
  training errors out.
- Unmerged branch `feat/wire-nan-features` (`936f7cb`) fixed the 4 non-whale names but **dropped whale
  flow** (now 0% NaN) → do NOT merge it.

## Goal
Train + enable a correct **5D** GMM regime classifier that includes the now-live whale-flow signal.

## Tasks
- [ ] Fix `train_regime_gmm.py` `FEATURE_COLUMNS` to the 5 real, Rust-aligned columns (FEATURES.md:327):
      `illiq_kyle_100, toxic_vpin_50, regime_absorption_zscore, trend_hurst_300, whale_net_flow_1h`.
      Add a `RUST_INFERENCE_ORDER` alignment constant; keep `suggest_labels` 5D.
- [ ] Port branch `936f7cb` training guards (kept at 5D): NaN-rate validation (>95% reject / >10% warn),
      min-rows guard (100), recursive `rglob`, `diagonal_relaxed` concat, `fill_nan→drop_nulls`.
- [ ] New `scripts/tests/test_train_regime_gmm.py` at **5D**: shapes `(N,5)`, `feature_count==5`, whale
      column in fixtures, keep missing-col / >95%-NaN-reject / min-rows / diagonal-concat tests, add a
      Python↔Rust feature-order alignment test. (Drop the branch's 4D / no-whale asserts.)
- [ ] **No Rust change** — master is already 5D-correct. Optional future hardening: dimension-agnostic
      `&[f64]` classifier (`regime.rs` + ~31 tests).
- [ ] Retrain on recent clean, schema-compatible days → generate + commit `models/regime_gmm.json`.
      (One clean day suffices to fit it; the 7-day streak gates alpha validation, not GMM clustering.
      Regime-model *quality* improves with market-condition diversity — retrain as clean days accrue.)
- [ ] Enable `gmm_model_path = "models/regime_gmm.json"` in `config/ing.toml:38`.
- [ ] After it lands, delete branch `feat/wire-nan-features` (its whale-drop is obsolete).

## Verify
- `pytest scripts/tests/test_train_regime_gmm.py -q` green (5D shapes, guards, alignment).
- Generated `regime_gmm.json`: `feature_names` = the 5 cols, `len(scaler_mean)==5`, `means[0]` len 5,
  weights sum ≈ 1.
- `cargo test -p ing regime` green; `RegimeClassifier::load` parses the JSON and `classify` returns a
  non-degenerate regime distribution on real features.
- `FEATURE_COLUMNS` order == the `gmm_input` order in `state/mod.rs:299-311`.
- No su-35 contact; no parquet-schema change (only model-input selection of existing features).

## Provenance
Derived from a two-agent investigation (Python training side + Rust inference side) on 2026-06-22.
Key finding: master's Rust inference is already 5D-correct with whale flow; the gap is the broken
training-script column names + disabled `gmm_model_path` + missing trained model artifact.
