"""XS — cross-sectional (Class 3) research layer.

Bar-level features and scores computed per pair against the pair's *own* history, so
~177 perps on wildly different price scales and liquidity can be ranked against each
other. See `docs/specs/maker_system.md` §5 and the `XS-*` rows in `docs/TASKS.md`.
"""
